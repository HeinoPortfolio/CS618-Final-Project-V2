-- General Notes About The Project:
    -- Changed the name of src/App.jsx to Blog.jsx
    -- Created a new src/App.jsx 
        -- used to import tools like tanstack, the query client and 
        the query provider 
    -- In module 6, created a 'pages' folder and moved or created the 
       following files:
       -- Blog.jsx (moved)
       -- 
    -- In module 8, renamed main.jsx to entry-client.jsx 

-- Environment Variables:
    -- DATABASE_URL=mongodb://localhost:27017/recipe-blog-v2
    -- VITE_BACKEND_URL="http://localhost:3000/api/v1"
    -- [For SSR] --> PORT=5173
    -- Fake database URL is:  
      -- DATABASE_URL=mongodb://localhost:27017/recipes-blog-simulated

-- Other

-- Useful Scripts
----- To create/login a user from DevTools:

const res = await fetch('http://localhost:3000/api/v1/user/signup', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'archie', password: 'dog' }),})

----- To login a user in DevTools:

const res = await fetch('http://localhost:3000/api/v1/user/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'mheino', password: 'archie' }),
})

----- In the frontend to run the distribution server use: serve dist
-- To run the frontend use:                     npm run dev

-- To run the backend use:                      npm start 

-- To start the backend in development mode:    npm run dev