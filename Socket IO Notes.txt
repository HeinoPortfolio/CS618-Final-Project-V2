General Notes:
--------------
-- List of parameters start with '?'
-- The query parameter for passing simple message in the URL is: 'mymsg'
-- The query parameter for the room (in the URL) is: 'room'
-- To add another parameter use '&' in the URL

Project Notes:
-------------- 
-- Install socket.io into the backend folder:
    -- npm install socket.io@4.7.2
-- Switch the database url to:
    -- will create a new database not a collection 
    -- DATABASE_URL=mongodb://localhost:27017/recipe-blog-v2-chat
-- In backend/app.js 
    -- Integrate the node server and socket.io 
-- Install socket.io client into frontend:
    -- npm install socket.io-client@4.7.2
-- Change the frontend "env" variable:
    -- VITE_SOCKET_HOST="localhost:3000"
    -- omit http need just the name of the server
-- Moved socket connection to a separate file:  backend/src/socket.js



